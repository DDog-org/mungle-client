import { theme } from '@daengle/design-system';
import { css } from '@emotion/react';

export const wrapper = css`
  padding: 0 0 calc(${theme.size.ctaButtonHeight} + 18px) 0;
`;

export const header = css`
  margin-bottom: 22px;
  padding: 18px;
`;
