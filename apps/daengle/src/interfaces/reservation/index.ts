export interface UserReservationGroomingList {
  estimateId: number;
  groomerName: string;
  petName: string;
  petImageUrl: string;
  shopName: string;
  reservedDate: string;
}

export interface UserReservationCareList {
  estimateId: number;
  vetName: string;
  petName: string;
  petImageUrl: string;
  reservedDate: string;
}
